"""第 3 章的交互式课程。"""

from __future__ import annotations

from minimem.learn.models import Apply, Card, Cliff, Explain, Lesson, Motto, Predict, Recall, Run

LESSON = Lesson(
    chapter=3,
    title="检索增强记忆基础",
    minutes=45,
    difficulty=2,
    prerequisites=[1],
    steps=[
        Motto("换成向量检索不会让你更强。向量真的懂语义，才会。"),
        Predict(
            question=(
                "三个方案在同一份数据上比召回：（1）第 1 章的字面词重叠；"
                "（2）**哈希向量**——形状是向量、走的是完整向量检索管线，但不懂语义；"
                "（3）真实句向量。哈希向量这一组会怎样？"
            ),
            options={
                "A": "明显好于字面匹配 —— 向量检索的架构本身就更优",
                "B": "和字面匹配差不多",
                "C": "比字面匹配还差",
            },
            answer="C",
            reveal=(
                "哈希向量（71.5%）**输给了**字面基线（79.2%），真实向量是 88.2%。"
                "这证明：「换成向量检索」这个架构动作本身不带来任何东西，"
                "全部改善都来自向量真的编码了语义。"
            ),
            trap=(
                "这是一个通用的验证手法，值得记住："
                "怀疑某个改进是不是真的时，**把关键组件换成一个形状相同但没有能力的假件**，"
                "看指标掉不掉。掉了，说明改进是真的；不掉，说明你改的不是关键。"
            ),
        ),
        Run(
            script="docs/chapter3/code/01_semantic_vs_literal.py",
            why="三方对照 + k 敏感性表。这是全书方法论最重要的一个实验。",
            look_for="分能力表里「多跳」那一行——语义检索在这里**反而更差**。",
            minutes=1,
        ),
        Predict(
            question="把 k 从 5 降到 1（只取最相关的一条），字面匹配和真实语义向量的总召回率会怎样？",
            options={
                "A": "语义向量优势更大 —— 它排序更准",
                "B": "两者几乎打平",
                "C": "字面匹配反超",
            },
            answer="B",
            reveal=(
                "k=1 时字面 70.8%、语义 70.1%，几乎打平；k=10 时才拉开到 86.1% 对 96.5%。"
                "**任何「A 比 B 好 X%」的说法，不指明 k 就没有意义**——"
                "而这个前提在厂商基准报告里经常缺席。"
            ),
        ),
        Predict(
            question=(
                "稠密向量在标识符上会翻车（XR-2049 和 XR-2050 几乎是同一个点）。"
                "那么 dense + BM25 的混合检索，在 MiniBench 上的总召回率会不会超过纯 dense？"
            ),
            options={
                "A": "会 —— 两路取长补短，融合必然更好",
                "B": "不会 —— 这个数据集测不出 BM25 的价值",
            },
            answer="B",
            reveal=(
                "hybrid 86.8%，纯 dense 88.2%——**融合略低**。"
                "因为 MiniBench 只有两个相似编号，稠密向量还分得开。"
                "但换成 12 个只差数字的编号做压力测试，dense 掉到 7/12，BM25 是 12/12。"
            ),
            trap=(
                "这里有一个比结论更重要的方法论："
                "**基准测不出某个机制的价值，不等于那个机制没价值**。"
                "很多论文说「我们的方法在 X 上提升了 Y%」，"
                "如果 X 本身测不到关键差异，那个 Y 就说明不了什么。"
            ),
        ),
        Run(
            script="docs/chapter3/code/02_hybrid_search.py",
            why="看向量空间里两个项目号有多近，以及 RRF 融合的中间过程。",
            look_for="消融那张表：bm25_floor_ratio 从 0 调到 0.5，召回率从 81.9% 回到 86.8%。",
            minutes=1,
        ),
        Recall(
            question="RRF 为什么只用名次而不用分数？这个设计又带来了什么坑？",
            answer=(
                "用名次是因为**量纲不同**：余弦相似度在 [-1,1]，BM25 得分可到几十且随语料变化，"
                "相加就必须先归一化，而任何归一化都要估计分布、引入新偏差。名次是无量纲的。\n"
                "坑在于：当某个通道对这个查询「无话可说」时，它返回的噪声依然享有"
                "满额的名次权重——「BM25 的第 2 名」和「dense 的第 2 名」一样重。"
                "解法是给弱通道设相对门槛（bm25_floor_ratio）。"
            ),
        ),
        Predict(
            question=(
                "把记忆的粒度从「一条消息一条」改成「10 条合并成一条」，召回率会从 81.9% 变成多少？"
            ),
            options={"A": "下降到 60% 左右", "B": "基本不变", "C": "上升到 100%"},
            answer="C",
            reveal=(
                "100%——但这个 100% 什么也没证明。全库只剩 3 条记忆，"
                "k=5 直接把整个库都返回了。它等价于「不检索，全塞进去」。"
                "同时准确率从 18.3% 掉到 4.3%，上下文从 85 token 涨到 500。"
            ),
            trap="**只看召回率会得出「粒度越粗越好」的荒谬结论。检索指标必须成组看。**",
        ),
        Run(
            script="docs/chapter3/code/03_granularity.py",
            why="粒度的三面：召回、准确、上下文成本。外加一笔噪声账。",
            look_for="噪声占比 80%——检索回来的 token 里，只有两成是有用的。",
            minutes=1,
        ),
        Recall(
            question="向量检索把第 1 章的「规模天花板」推倒了吗？",
            answer=(
                "没有推倒，只是推远了。两条曲线**都是线性的**——"
                "暴力向量检索仍要算查询与全部 N 条的内积。"
                "它降的是**常数**（numpy 矩阵乘对上 Python 循环，实测约一个数量级以上）。"
                "真正的次线性需要 ANN 索引，代价是召回不再是 100%，"
                "经验分界大约在百万条。"
            ),
        ),
        Explain(
            task="向同事解释：为什么 2026 年了，检索系统还要保留 BM25 这个 1990 年代的算法。",
            checklist=[
                "说出稠密向量的两类结构性盲区（标识符、罕见词），并解释为什么是结构性的",
                "说明「意思相近」和「是同一个东西」的区别",
                "给出混合检索的代价（两套索引、延迟约 1.5~2 倍）",
                "说出什么场景下**不**需要混合检索",
            ],
        ),
        Apply(
            level="🟢",
            task="把 VectorMemory 的 mode 依次设为 dense / bm25 / hybrid，跑第 10 章的 harness，对比七列。",
            starting_point="docs/chapter10/code/01_harness.py",
            success_looks_like="你能说出每种模式赢在哪一类查询上，以及各自的延迟差异",
        ),
        Apply(
            level="🟡",
            task=(
                "给 VectorMemory 加一个 rerank 步骤：检索 top-20 后，"
                "用一个交叉编码器重排取 top-5。测量它带来的召回提升和延迟增加。"
            ),
            starting_point=(
                "模型：python -m minimem.utils.fetch_model BAAI/bge-reranker-base（约 1.1GB）"
            ),
            success_looks_like="准确率上升、延迟上升几十到几百毫秒；你能判断这笔交易值不值",
        ),
        Apply(
            level="🔴",
            task=(
                "MiniBench 太小，测不出很多差异。**给它加 30 个你自己业务场景的查询**，"
                "按八类能力标注，然后重跑所有对比。看结论会不会翻转。"
            ),
            starting_point="minimem/eval/dataset.py",
            success_looks_like=("你有了一个属于自己的评测集——这比本书给的任何结论都有价值"),
            verify="python -m pytest tests/test_eval.py -q",
        ),
        Cliff(
            text=(
                "第 1 章的三堵墙，现在推倒了一堵、推远了一堵。"
                "**第三堵原封不动**：VectorMemory 依然会把「星辰银行」和「长风科技」"
                "两条矛盾的记忆一起返回，并且不知道哪条已经过期。"
            ),
            next_chapter="第 4、5 章：当记忆之间有关系、有时间。",
        ),
    ],
    cards=[
        Card(
            front="怎么验证一个改进是真的有效，而不是心理作用？",
            back=(
                "把关键组件换成「形状相同但没有能力」的假件（如哈希向量之于真实句向量），"
                "看指标掉不掉。掉了说明改进真实；不掉说明你改的不是关键部件。"
            ),
            chapter=3,
            tag="方法论",
        ),
        Card(
            front="稠密向量的结构性盲区有哪些？为什么换更大的模型也解决不了？",
            back=(
                "标识符（XR-2049 vs XR-2050）、罕见词与新词、否定式。"
                "解决不了是因为这不是容量问题——从语义上说这些文本**确实**相似，"
                "模型忠实地反映了这一点。该用 BM25 的地方还是要用。"
            ),
            chapter=3,
        ),
        Card(
            front="RRF 只用名次不用分数，好处和代价各是什么？",
            back=(
                "好处：绕开量纲问题（余弦在 [-1,1]，BM25 可到几十），名次天然可比。"
                "代价：通道质量不对等时会被噪声劫持——一个「无话可说」的通道，"
                "它的第 2 名和另一通道的第 2 名权重相同。需要给弱通道设相对门槛。"
            ),
            chapter=3,
        ),
        Card(
            front="为什么说「只看召回率的检索对比是无效的」？",
            back=(
                "因为把 k 调大、把粒度合粗都能刷高召回率，代价是准确率下降和上下文变贵。"
                "极端情况：全库合并成 3 条，召回率 100%，但那等价于「不检索，全塞进去」。"
                "必须同时看准确率和 ctx token。"
            ),
            chapter=3,
        ),
        Card(
            front="检索的性能瓶颈通常在哪？",
            back=(
                "**不在你以为的地方，要靠 profile 找**。本书踩过的两个真实的坑："
                "用 argsort 对全部候选排序而不是 argpartition 取前 k；"
                "纯 dense 模式下也无条件构建了 BM25 索引。"
                "两处修完，5 万条的检索延迟从 35ms 降到 10ms——和索引结构毫无关系。"
            ),
            chapter=3,
        ),
    ],
)
