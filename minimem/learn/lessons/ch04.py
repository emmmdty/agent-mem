"""第 4 章的交互式课程。"""

from __future__ import annotations

from minimem.learn.models import Apply, Card, Cliff, Explain, Lesson, Motto, Predict, Recall, Run

LESSON = Lesson(
    chapter=4,
    title="结构化与图记忆",
    minutes=40,
    difficulty=2,
    prerequisites=[3],
    steps=[
        Motto("向量检索会「找最像的」，但不会「走一步」。多跳问题要的是后者。"),
        Predict(
            question=(
                "给记忆建一张实体关系图，用 Personalized PageRank 检索。"
                "和第 3 章的向量方案比，**总召回率**会怎样？"
            ),
            options={
                "A": "大幅提升 —— 图就是为关系设计的",
                "B": "略有提升",
                "C": "大幅下降 —— 纯图检索总分会惨败",
            },
            answer="C",
            reveal=(
                "纯图检索（PPR）总召回只有 37.5%，而向量方案是 86.8%。"
                "它在「同义改写」「时序」「知识更新」「归纳」四类上**全是 0%**。\n"
                "原因不是效果差，是**根本没有输出**：这些查询里没有可抽取的实体，"
                "PPR 连种子都没有。"
            ),
            trap=(
                "但如果就此认为「图记忆没用」，也错了。"
                "看分能力表：多跳那一行，图检索把 q10 从 50% 提到了 100%。"
                "**一个在某项能力上大幅领先的方案，可能在总分上惨败**——"
                "这正是为什么本书坚持看分能力表而不是总分。"
            ),
        ),
        Run(
            script="docs/chapter4/code/01_multihop.py",
            why="看图检索在哪一题上赢、在哪一题上完全失效。",
            look_for="逐题细看那两道多跳题：q10 图赢了，q11 图返回空。",
            minutes=1,
        ),
        Recall(
            question="为什么纯图检索在「同义改写」类查询上得分是 0，而不是「差一些」？",
            answer=(
                "因为它不是效果差，是**没有输出**。这类查询（「我平时业余时间干什么？」）"
                "里没有任何可抽取的实体，PPR 没有种子就无法启动，直接返回空列表。\n"
                "「召回了但排序不好」可以调参救，「根本没启动」只能靠另一条通道兜底——"
                "这就是 GraphMemory 默认用 hybrid 的原因。"
            ),
        ),
        Predict(
            question=(
                "PPR 会给图上每个连通节点分配一点分数，哪怕小到第 1 名的十万分之一。"
                "把 PPR 通道和向量通道做 RRF 融合时，这些近乎为零的候选会怎样？"
            ),
            options={
                "A": "没影响，它们排在后面",
                "B": "会挤掉向量通道里真正相关的结果",
            },
            answer="B",
            reveal=(
                "关掉门槛时多跳召回是 50%，加上门槛后回到 75%。"
                "因为 **RRF 只看名次**——「PPR 的第 3 名」和「向量的第 3 名」权重相同，"
                "哪怕前者的分数比第 1 名小五个数量级。"
            ),
            trap=(
                "**这是第 3 章那个 BM25 陷阱的第二次出现。** 规律值得记住：\n"
                "任何要参与 RRF 融合的通道，都必须先清理自己的低质量候选。\n"
                "但门槛值不能照抄——PPR 用 0.01，BM25 用 0.5，差两个数量级，"
                "因为 PPR 分数按跳数指数衰减。"
            ),
        ),
        Run(
            script="docs/chapter4/code/02_ppr_and_the_trap.py",
            why="看 PPR 的分数怎么一跳跳传出去，以及门槛消融的效果。",
            look_for="关掉门槛后 PPR 通道的第 3~5 名——分数五个数量级之差，内容完全无关。",
            minutes=1,
        ),
        Predict(
            question=(
                "把规则抽取换成 LLM 抽取（能抓到「毛毛」「灵山」这类裸专名，图更密），"
                "多跳召回率会怎样？"
            ),
            options={
                "A": "提升 —— 抽取质量更好，图更完整",
                "B": "基本不变",
                "C": "下降",
            },
            answer="C",
            reveal=(
                "多跳从 75% 掉到 50%。因为 LLM 抽取把 XR-2049 和 XR-2050 **都**"
                "连到了「项目」这个泛称节点上；PPR 从「项目」出发时分数被两个项目均分，"
                "于是一跳可达的 m08 压过了要走两跳的 m07。\n"
                "**枢纽节点会稀释分数。**"
            ),
            trap=(
                "结论：**「更好的抽取」不等于「更好的检索」**。"
                "图的效果取决于抽取质量、图结构、查询锚定三者的配合，"
                "单独优化一个，另外两个可能反过来拖后腿。"
                "这和第 3 章「换成向量检索这个架构动作本身不带来任何东西」是同一类教训。"
            ),
        ),
        Run(
            script="docs/chapter4/code/03_extraction_cost.py",
            why="算清建图的三块成本：抽取、检索、以及最容易被忽略的人工。",
            look_for="「折算到每万条记忆」那一行，以及末尾记录的两个真实 bug。",
            minutes=1,
        ),
        Recall(
            question="图记忆的三块成本分别是什么？哪一块最容易被忽略？",
            answer=(
                "1. **抽取**：LLM 版每条记忆一次调用；"
                "2. **检索**：PPR 每次在整张图上幂迭代，且不像向量那样能靠 ANN 降复杂度；"
                "3. **人工**：规则版的领域词表、姓氏表、消解规则，换个业务领域全部重写。\n"
                "第三块最容易被忽略，因为它不出现在账单上——而且**它不随规模摊薄**。"
            ),
        ),
        Explain(
            task="向同事解释：我们的多跳问答效果不好，该不该上图记忆？",
            checklist=[
                "先说清楚多跳为什么是向量检索的结构性弱点（没有「走一步」这个动作）",
                "说出纯图检索的失效场景（查询里抽不出实体），以及为什么必须混合",
                "列出三块成本，特别是人工维护词表这块",
                "给出判断顺序：先看分能力表，再上规则抽取，最后才考虑 LLM 抽取",
            ],
        ),
        Apply(
            level="🟢",
            task="把 damping 从 0.85 改成 0.5 和 0.99，重跑实验二，看分数传播的距离怎么变。",
            starting_point="docs/chapter4/code/02_ppr_and_the_trap.py 里 build() 的参数",
            success_looks_like="damping 小时分数几乎不出种子，大时传得远但更发散",
        ),
        Apply(
            level="🟡",
            task=(
                "实现 node specificity：给实体节点按「它连接了多少条记忆」降权（类似 IDF），"
                "抑制枢纽节点稀释分数的问题。重跑实验三，看 LLM 抽取版的多跳能否超过规则版。"
            ),
            starting_point="minimem/graph.py 的 _ppr_rank，修改 personalization 的权重分配",
            success_looks_like="q10 在 LLM 抽取版下召回率回到 100%",
            verify="python -m pytest tests/test_graph.py -q",
        ),
        Apply(
            level="🔴",
            task=(
                "给 GraphMemory 加实体消解：把「星辰银行」「星辰」「那家银行」合并成同一节点。"
                "用向量相似度做候选、规则或 LLM 做判定，然后测量它对多跳召回的影响"
                "**以及它引入的错误合并率**。"
            ),
            starting_point="minimem/utils/entities.py 与 minimem/graph.py 的 _add",
            success_looks_like="一条「合并率 vs 错误合并率」的权衡曲线——这才是实体消解的真实难度",
        ),
        Cliff(
            text=(
                "图能顺着关系走了，但它仍然不知道**哪条事实已经过期**。"
                "更糟的是，「星辰银行」和「长风科技」现在还在图上连在一起，"
                "看起来比以前更「相关」了。"
            ),
            next_chapter="第 5 章：给记忆装上时间轴。",
        ),
    ],
    cards=[
        Card(
            front="为什么多跳查询是向量检索的结构性弱点，而不是调参问题？",
            back=(
                "向量检索做的是「找和整个问句最像的几条」，它没有「走一步」这个动作。"
                "多跳的第二跳记忆在语义上离问句很远，排不进前列。"
                "换更大的嵌入模型也解决不了——这不是相似度算得准不准的问题。"
            ),
            chapter=4,
        ),
        Card(
            front="Personalized PageRank 和普通 PageRank 差在哪？",
            back=(
                "只差一件事：随机跳转不是跳到任意节点，而是跳回**种子集合**（查询里抽出的实体）。"
                "这让分数从查询相关的位置出发沿边传播、逐跳衰减——"
                "这就是「顺着关系走」的数学形式。"
            ),
            chapter=4,
        ),
        Card(
            front="纯图检索的总召回率为什么可能远低于向量检索？",
            back=(
                "因为它要求查询里有可锚定的实体，而日常提问大量使用泛称与代词"
                "（「我平时业余时间干什么？」）。抽不出种子，PPR 无法启动，返回空——"
                "这不是「效果差」而是「没有输出」。所以图记忆必须配向量通道兜底。"
            ),
            chapter=4,
        ),
        Card(
            front="为什么「更好的抽取」不一定带来「更好的检索」？",
            back=(
                "实测：LLM 抽取建了更密的图，多跳召回反而从 75% 掉到 50%。"
                "因为它把多个项目都连到「项目」这个泛称节点上，PPR 从枢纽出发时分数被均分，"
                "二跳的正确答案竞争不过一跳的无关结果。**枢纽节点会稀释分数。**"
            ),
            chapter=4,
        ),
        Card(
            front="为什么 PPR 通道的融合门槛比 BM25 通道小两个数量级？",
            back=(
                "PPR 分数按跳数**指数衰减**——二跳邻居天然比种子低几个数量级，"
                "而它们往往正是答案。BM25 的分数分布平缓得多。"
                "门槛必须按通道自身的分数分布定，照抄会把真结果一起滤掉。"
            ),
            chapter=4,
        ),
    ],
)
