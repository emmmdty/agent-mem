# 贡献指南

感谢你愿意为 `agent-mem` 出力。这个领域变化快、噪音大，**一条经过验证的勘误，价值高于一段新写的内容**。

## 最需要的四类贡献

1. **勘误**：论文编号、作者、数字、结论引用错误。请附上原文出处（arXiv 编号 + 具体表格/章节）。
2. **复现结果**：你在自己机器上跑某章实验的结果，尤其是与教程结论不一致的情况。请附环境信息与完整命令。
3. **时效性更新**：标了 🕐 的表格（框架对比、API 细节、产品特性）过期了。请在 PR 里更新「截至」日期。
4. **新论文/新框架**：请说明它引入了什么**既有方案没有的机制**，而不只是「又一个记忆库」。

## 提交前的检查清单

```bash
pip install -e ".[dev]"
ruff check .            # 代码风格
ruff format --check .   # 格式
pytest                  # 测试（不含 slow / needs_api / needs_gpu 标记）
```

文档改动请额外确认：

- 章节内引用的图片路径是相对路径，且图片放在该章的 `images/` 下。
- 新增章节文件后，已同步更新 `docs/_sidebar.md`。
- `docsify serve docs` 本地打开无渲染异常。

## 写新章节？

先读 **[CHAPTER_GUIDE.md](./CHAPTER_GUIDE.md)**——它有完整的章节模板、`MemoryStore` 契约、交互式课程的写法，以及并行协作时的文件所有权约定。已完成的第 1、2、3、10 章是参考实现。

下面是所有贡献都适用的通用规范。

## 内容写作规范

### 1. 数字必须带来源标记

| 标记 | 用法 |
| :--- | :--- |
| 📄 | 论文原文。写成：📄 论文报告 X（arXiv:XXXX.XXXXX, Table 2） |
| 🏢 | 厂商自述，无第三方复现。必须显式写「厂商自述」 |
| ⚠️ | 存在争议：不同来源矛盾，或已被独立审计质疑。两方数字都要列 |
| 🕐 | 时效性内容。必须写「截至 YYYY-MM」 |

**反例**（不要这么写）：「Mem0 比全上下文方案准确率高 26%」。
**正例**：「🏢 Mem0 论文报告其相对全上下文基线的相对提升（arXiv:2504.19413）；⚠️ 该结论所依据的 LoCoMo 基准被独立审计指出存在答案键错误与评判宽松问题，详见第 10 章。」

### 2. 每个方案必须写代价

新增一种记忆机制时，至少回答：写入一条记忆要几次 LLM 调用？检索一次要多少 token？相比上一章的方案，延迟是几倍？

### 3. 术语

- 首次出现的英文术语给出中文译名与英文原文，如「巩固（consolidation）」。
- 全书统一用「表征 / 操作 / 载体」三轴，不要混用心理学四分法作为模块划分。
- 已确定的译名见附录 D 术语表；新增术语请同步补充到术语表。

### 4. 代码规范

- 章节示例脚本放在 `docs/chapterN/code/`，可独立运行，文件名带序号：`01_xxx.py`。
- 复用性代码放进 `minimem/` 包，并配 `tests/` 下的测试。
- 示例脚本不得硬编码 API Key，一律从环境变量读取。
- 需要联网/GPU/大模型的脚本，在文件头注释里写清前置条件与预计耗时。

## Git 工作流

### 分支

| 前缀 | 用途 | 示例 |
| :--- | :--- | :--- |
| `feat/chNN-<slug>` | 新章节或章节主功能 | `feat/ch04-graph-memory` |
| `docs/<slug>` | 非章节文档 | `docs/env-setup` |
| `fix/<slug>` | 勘误与 bug 修复 | `fix/ch03-bm25-tokenizer` |
| `chore/<slug>` | 工程配置 | `chore/ci-linkcheck` |

### 提交信息（Conventional Commits）

```
<type>(<scope>): <中文描述>

type:  feat | fix | docs | test | chore | ci | refactor
scope: ch01~ch10 | minimem | eval | site | repo
```

示例：

```
feat(ch03): 实现 BM25 与 dense 的混合检索与 RRF 融合
fix(ch01): 修正 Du et al. 综述的 arXiv 编号
docs(site): 补充 docsify 侧边栏第 4~5 章条目
```

### 合并

- 章节分支完成后以 `--no-ff` 合入 `main`，保留章节开发边界。
- CI 全绿才合并。
- 涉及结论变更的 PR，请在描述里写清「原结论 → 新结论 → 依据」。

## 提 Issue

用仓库的 Issue 模板：**内容勘误**、**代码/环境问题**、**新增内容建议**。勘误类请务必附出处，这能省下双方大量核对时间。

## 许可

提交贡献即表示同意：文档内容以 [CC BY-NC-SA 4.0](./LICENSE.txt) 授权，代码以 [MIT](./LICENSE-CODE) 授权。
