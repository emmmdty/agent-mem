# 第 8 章：经验与技能记忆
#
#   pip install -e ".[vector]"   # 句向量（relevance 那一项需要）
#   python -m minimem.utils.fetch_model
#
#   python docs/chapter8/code/01_importance.py             # 三项打分值不值
#   python docs/chapter8/code/02_reflection_and_skills.py  # 反思与技能库
#
# 两个脚本各几秒，全部离线：LLM 用 ScriptedLLM 模拟。
# **模拟的是格式与用量，不是判断力**——反思质量与归纳泛化度换成真模型会变，
# 但成本结构（打分每条一次、反思低频高价）不变。正文对此有专门说明。
#
# 想用真模型跑：pip install -e ".[llm]"，配好 .env，把脚本里的
# make_llm(meter) 换成 get_llm(meter=meter)。
