# agent-mem 插图提示词（GPT Image 2）

> 生成日期：2026-07-31　|　仓库：https://github.com/emmmdty/agent-mem
> 本文件是**工作文档**，插图落地后可删除。

---

## 这里面只有「需要出图」的那些

全书配图分两拨，**这份文件只管第二拨**：

| | 内容 | 状态 |
| :--- | :--- | :--- |
| **结构图 / 数据图** | 20 张 mermaid（各章正文内联）+ 5 张 matplotlib（`docs/images/`） | ✅ **已完成，不需要提示词** |
| **插图 / 视觉** | 封面、章题图、概念隐喻 | ⬇️ 本文件，共 15 条提示词 |

已完成的那 25 张：mermaid 直接写在各章 markdown 里，随白天/夜间主题自动换色；
数据图由 `tools/make_figures.py` 生成，数字全部抄自各章实测表格，
每张出 `-light` / `-dark` 两份 SVG。改数字重跑 `python tools/make_figures.py` 即可。

---

## 通用风格前缀（每次对话粘贴一次）

```
你在为一本中文开源技术书《agent-mem —— 从零开始学 Agent 记忆》画插图。
仓库：https://github.com/emmmdty/agent-mem

统一视觉规范（所有图都要遵守）：
- 风格：克制的编辑式矢量插画，扁平着色，几何感，留白充足。
- 主色：蓝色 #3f7ef0。辅助：中性灰。强调/警告：低饱和红。每张图颜色不超过三种。
- 禁止元素：大脑、神经元、机器人、电路板、发光特效、赛博朋克、3D 渲染感、
  骷髅头、生化危害标志。这本书的立场是「讲机制不讲口号」，视觉上同样要克制。
- 每张图都出两版：白底版，和深色底版（底色 #14181f）。
- 图中如需文字，一律用简体中文，字体要清晰可读，不要出现英文占位词。
- 尺寸按每条提示词的指定。
```

---

## 一、封面与社交预览（2 条）

### P1　封面主视觉
```
16:9 横版，技术书封面插画。
画面：三层半透明的水平面板在纵深方向堆叠——最上层明亮、密布发光的小数据点；
中间层暗一些、点稀疏；最下层近乎全黑，只剩几个微弱的点。
层与层之间有细的双向箭头线，暗示「降级与回捞」，而不是「删除」。
不要任何文字。构图要在缩小到 400px 宽时依然清晰。
```

### P2　社交预览图（GitHub / 微信分享卡片）
```
1200×630，横版。
左侧三分之二留白，用于后期叠加书名文字（你不要画文字）。
右侧三分之一：P1 的三层结构的简化版，只保留三个层面和层间的双向箭头。
整体极简，大量留白，白底版与 #14181f 深色底版各一张。
```

---

## 二、章题图（10 条，统一风格）

> 每章开头一张窄横幅（建议 1600×400），只表达该章的核心张力，不承担信息量。
> 十张必须是同一套视觉语言：同样的几何语汇、同样的三色限制、同样的留白比例。

### P3　第 1 章 · 为什么 Agent 需要记忆
```
1600×400 窄横幅。左边一条完整连续的对话气泡序列，向右逐渐褪色、断裂、消失，
最右端只剩一个空的气泡轮廓。表达「无状态」不是故障而是设计。
不要文字。白底版与深色底版各一张。
```

### P4　第 2 章 · 上下文与长上下文记忆
```
1600×400 窄横幅。一个固定宽度的矩形窗口在一长串等距方块上向右滑动，
窗口左侧滑过的方块直接消失（不是变暗，是消失）。
最左端有三个方块颜色不同——它们是最先消失的，但恰恰最重要。
不要文字。白底版与深色底版各一张。
```

### P5　第 3 章 · 检索增强记忆基础
```
1600×400 窄横幅。左侧一个查询点发出射线，射入右侧一片散布的点云，
命中其中少数几个点并使其高亮。点云中有一个高亮点的色调略有不同——它是误召回。
不要文字。白底版与深色底版各一张。
```

### P6　第 4 章 · 结构化与图记忆
```
1600×400 窄横幅。一张稀疏的节点—连线网络，其中两个不相邻的节点通过中间一个
共享节点连通，这条两跳路径被高亮成蓝色，其余连线是浅灰。
不要文字。白底版与深色底版各一张。
```

### P7　第 5 章 · 时间感知与知识演化
```
1600×400 窄横幅。两条平行的水平时间轴，上下排列。
上轴上的标记点与下轴上对应的标记点之间用细斜线相连，斜线的倾斜方向不一致——
有的向右倾（事实晚于陈述生效），有一条明显向左倾（事实早于陈述生效）。
不要文字。白底版与深色底版各一张。
```

### P8　第 6 章 · agentic memory 与自组织
```
1600×400 窄横幅。左侧一堆杂乱重叠的卡片，向右经过一道分选，
变成右侧少数几张整齐、互不重叠的卡片；被合并掉的卡片以极淡的残影留在原处，
暗示「干净是有代价的——历史丢了」。
不要文字。白底版与深色底版各一张。
```

### P9　第 7 章 · OS 式分层记忆与调度
```
1600×400 窄横幅。三条水平带自上而下明度递减，带与带之间有双向箭头。
最上面那条带的宽度明显受限（画一对夹住它的边界线），表示硬性预算上限。
不要文字。白底版与深色底版各一张。
```

### P10　第 8 章 · 经验与技能记忆
```
1600×400 窄横幅。左侧一条曲折的路径（试错轨迹），向右被提炼成一条笔直的、
带若干等距节点的直线（可复用流程），直线上有几个空心圆表示参数槽。
不要文字。白底版与深色底版各一张。
```

### P11　第 9 章 · 参数化记忆与持续学习
```
1600×400 窄横幅。一片密集均匀的网格（权重矩阵的抽象），
其中一小块区域被改动、颜色不同；改动处向外扩散出几圈涟漪，
涟漪触及的远处节点出现了不协调的色差。
不要文字。白底版与深色底版各一张。
```

### P12　第 10 章 · 评测、安全与工程落地
```
1600×400 窄横幅。一排整齐的相同卡片，其中三张色调略有不同、混在中间毫不显眼。
一道水平扫描光束掠过，被扫到的那三张异色卡片亮起并占据前排，
旁边正常的卡片相应变暗。
不要文字。白底版与深色底版各一张。
```

---

## 三、概念隐喻插图（3 条，正文内使用）

> 这三张进正文，允许带中文标注——Image 2 的中文渲染可用。
> 但仍建议只标 2~4 个短词，长句仍交给正文。

### P13　相似不等于相同（第 3 章 3.3.1）
```
正方形。一片散布的灰色小圆点构成向量空间；其中两个点几乎重叠，被高亮，
它们之间用一条极短的测量线连接，标注「距离很近」。
但一条淡淡的分界线揭示这两个点分属不同的组，分别标注「正确」与「错误」。
中文标注共三处，均为 2~4 字，字体清晰。白底版与深色底版各一张。
```

### P14　涟漪效应（第 9 章 9.3.1）
```
正方形。一片规则的节点网格。中心一个节点被明确改动（蓝色高亮），
标注「已编辑」。改动向外扩散出同心涟漪。
涟漪范围内的节点已随之更新；但边缘有两三个节点仍保持旧状态、
用低饱和琥珀色标出，标注「旧推论还在」。
中文标注两处。白底版与深色底版各一张。
```

### P15　潜伏的投毒（第 10 章 10.8）
```
16:9 横版。一个整齐的卡片网格代表记忆库，其中三张色调略深、
安静地混在其中，标注「写入时无害」。
右侧同一批卡片，一道检索光束扫过，那三张卡片亮起并挤到前排，
旁边真实的卡片被挤出画面边缘，标注「检索时发作」。
不要骷髅、不要病毒图标、不要爆炸。中文标注两处。
白底版与深色底版各一张。
```

---

## 落地说明

- **图片放** `docs/images/`。命名建议 `cover.png`、`ch01-banner.png`、`fig-3-4-similarity.png`。
- **章题图**若要用，在每章 markdown 的标题下一行插入，与数据图同样的双份写法：
  ```html
  <img class="fig-light" src="images/ch01-banner-light.png" alt="第一章题图">
  <img class="fig-dark" src="images/ch01-banner-dark.png" alt="第一章题图">
  ```
  站点已有 `.fig-light` / `.fig-dark` 的主题切换 CSS，无需再改。
- **优先级**：P1、P2（封面与分享卡）最先做，收益最直接；P13~P15 三张概念图次之；
  十张章题图属于锦上添花，风格必须统一，建议一次性做完再挑。
- **验收**：中文有没有缺笔错字、缩到 400px 宽还认不认得出、深色版在 #14181f 上对比度够不够。
